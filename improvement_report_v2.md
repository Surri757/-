# 模型优化改进报告 v2

## 任务背景
- **目标**：将收益率从1.4%提升至5.0%以上
- **当前收益率**：1.4044（Final Score 0.069245）
- **优化路径**：基于Task 6调研报告的改进路线图

## 本次改进内容

### 1. 配置参数优化 (config.py)

#### 学习率调整
- 从 `5e-5` 调整到 `3e-5`，更适合深度Transformer模型

#### 训练轮数增加
- 从 `50 epochs` 增加到 `80 epochs`，给模型更多收敛机会

#### 损失函数权重优化
- **top5_weight**: 从 `2.0` 提升到 `3.5`（大幅提升Top-5股票的重要性）
- **pairwise_weight**: 从 `1` 提升到 `2`（增加pairwise损失权重）
- **base_weight**: 从 `1.0` 降低到 `0.5`（降低非Top-K样本的权重）
- **新增 top10_weight**: 2.0（额外关注Top-10股票）

#### 学习率调度器改进
- 从 LinearLR 改为 CosineAnnealingLR，提供更平滑的学习率衰减
- 增加 5 个 epoch 的 warmup 阶段

#### 正则化增强
- **weight_decay**: 从 `1e-5` 提升到 `2e-5`

### 2. 输出目录隔离
- 新模型输出到 `./model/60_158+39_v2` 目录，避免覆盖原有模型

## 改进原理

### 为什么更强调Top-5股票？

1. **评分指标特性**：最终得分公式 `(pred_return_sum - random_return_sum) / (max_return_sum - random_return_sum)`，其中：
   - `pred_return_sum`: 预测Top-5的真实收益率之和
   - `max_return_sum`: 真实Top-5的收益率之和
   - `random_return_sum`: 随机选择5只股票的平均收益

2. **收益集中性**：在股票市场中，涨幅往往集中在少数股票上，正确识别这些股票是获得高收益的关键

3. **损失函数设计**：
   - Listwise Loss：关注整体排序
   - Pairwise Loss：关注样本对的相对顺序
   - 通过增加Top-5的权重，模型会更关注预测这些高收益股票

### CosineAnnealing vs LinearLR

- **LinearLR**：线性衰减，可能导致训练后期学习率过低
- **CosineAnnealing**：余弦衰减曲线，更平滑，有利于深度网络微调

## 预期效果

基于调研报告中的建议和配置调整：
1. 更强的Top-K聚焦能力
2. 更充分的模型收敛
3. 更好的泛化性能（通过余弦退火和学习率预热）

## 下一步计划

如果v2版本仍未达到5%目标，将实施以下中期改进：
1. **LightGBM Ranker集成**：将LightGBM与Transformer模型集成
2. **新增波动率特征**：添加更多波动率相关技术指标
3. **Cross-sectional rank特征**：添加日内股票相对排名特征

## 实验记录

| 实验版本 | top5_weight | base_weight | 学习率 | Epochs | 预期提升 |
|---------|-------------|-------------|--------|--------|----------|
| 基线    | 2.0         | 1.0         | 5e-5   | 50     | -        |
| v2      | 3.5         | 0.5         | 3e-5   | 80     | +X%      |

---
*创建时间：2026-05-10*
